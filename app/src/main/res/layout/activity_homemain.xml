<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/couple_pink_bg"
    tools:context=".ui.activities.HomeMainActivity">

    <!-- ViewPager2 để hi�����n thị các fragments -->

    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/viewPager"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.0" />


        <com.example.couple_app.ui.views.DraggableImageView
            android:id="@+id/ic_chatbot_fab"
            android:layout_width="90dp"
            android:layout_height="90dp"
            android:background="@drawable/chatbot_fab_bg"
            android:clickable="true"
            android:contentDescription="@string/chatbot_label"
            android:focusable="true"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_chatbot"
            android:layout_marginRight="250dp"
            android:layout_marginTop="100dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="@+id/viewPager" />


</androidx.constraintlayout.widget.ConstraintLayout>
